// 14.1.cpp : Defines the entry point for the console application.
//Пользователь вводит строку с клавиатуры в фиксированный массив. Необходимо проверить, cколько элементов массива теперь занято и сколько свободно.

#define _CRT_SECURE_NO_WARNINGS

#include "stdafx.h"
#include <iostream>
#include <string.h>

using namespace std;

int _tmain(int argc, _TCHAR* argv[])
{
	const int size = 128;
	char cmas[size];

	string str;

	cout << "Enter the line: ";
	cin.getline(cmas, size);

	cout << "\n\n";
	cout << "There is " << size - strlen(cmas) << " symbols free.\n\n";
	cout << "And " << strlen(cmas) << " symbols oqqupied.\n\n";


	return 0;
}

